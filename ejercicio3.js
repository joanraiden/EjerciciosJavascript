// Definimos dos variables numéricas
let n1 = 10;
let n2 = 12;

// Usamos una estructura if para comparar si son iguales o no, para luego mostrar en consola el mensaje dependiendo de cual if sea correcto
if (n1 == n2) {
    console.log(`${n1} y ${n2} son iguales`);
} else {
    console.log(`${n1} y ${n2} no son iguales`);
}